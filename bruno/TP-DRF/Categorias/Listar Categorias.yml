info:
  name: Listar Categorias
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/gastos/categorias/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
