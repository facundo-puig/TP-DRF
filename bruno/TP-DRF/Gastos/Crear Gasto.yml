info:
  name: Crear Gasto
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/gastos/"
  body:
    type: json
    data: |-
      {
          "descripcion": "Compra de supermercado",
          "monto": 15000.50,
          "categoria": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
