info:
  name: Editar Gasto
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/gastos/1/"
  body:
    type: json
    data: |-
      {
          "descripcion": "Compra actualizada",
          "monto": 20000,
          "categoria": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
